package ${package}.${moduleName}.param.base;

import lombok.Data;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Data
public class PageDTO {
	private int currentPage = 1;
	private int pageSize = 10;
}
